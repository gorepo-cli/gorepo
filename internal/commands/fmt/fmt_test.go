package fmt

import "testing"

func TestFmt(t *testing.T) {
	t.Run("general", func(t *testing.T) {
		t.Run("should fail if there is no work.toml", func(t *testing.T) {})
	})
	t.Run("should run command in all modules", func(t *testing.T) {})
}
